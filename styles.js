import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    name: {
        fontWeight: 'bold',
        fontSize: 36,
        marginTop: 45,
        textAlign: 'center',
    },
    sliderTrackStyle: {
        backgroundColor: '#ff6a00',
        height: 20,
        borderRadius: 20,
    }
});

